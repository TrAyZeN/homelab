---
- name: Provision
  hosts: servers
  vars:
    ssh_port: 22
    ssh_user: "vagrant"

  tasks:
    - name: Update repositories and upgrade packages
      become: true
      apt:
        update_cache: yes
        name: "*"
        state: latest
  roles:
    - sshd
